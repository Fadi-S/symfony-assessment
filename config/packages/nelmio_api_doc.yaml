nelmio_api_doc:
    documentation:
        info:
            title: Symfony Assessment
            description: These are the endpoints for my Symfony Assessment
            version: 1.0.0
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
        security:
            - Bearer: [ ]
    areas:
        path_patterns:
            - ^/api(?!/doc$)
